
package com.axiomalaska.crks.service;

import junit.framework.TestCase;

public class TestGetLayerSLD extends TestCase {
    /*
    public static void testGetLayerSLD(){
        VectorLayer layer = (VectorLayer) HibernateUtil.currentSession().createCriteria( VectorLayer.class )
        .add( Restrictions.isNotNull("sld") ).setMaxResults( 1 ).uniqueResult();
        
        String xml = CrksService.getLayerSLD( layer.getId() );
    	assertNotNull( xml );
    	assertTrue( xml.length() > 0 );
    }
   */
    
    public static void testFakeTest(){
    	assertTrue( true ); //amazing
    }
}
